spring:
  application:
    name: Vyapar
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/monopoly}
    username: ${DB_USERNAME:rajshaikh}
    password: ${DB_PASSWORD:Sam@2445}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:validate}
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: ${PORT:9090}
