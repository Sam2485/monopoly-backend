spring:
  application:
    name: Vyapar
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/monopoly}
    username: ${SPRING_DATASOURCE_USERNAME:rajshaikh}
    password: ${SPRING_DATASOURCE_PASSWORD:Sam@2445}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:9090}
