spring:
  application:
    name: Vyapar
  datasource:
    url: ${DATABASE_URL:${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/monopoly}}
    username: ${DB_USERNAME:${SPRING_DATASOURCE_USERNAME:rajshaikh}}
    password: ${DB_PASSWORD:${SPRING_DATASOURCE_PASSWORD:Sam@2445}}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:validate}
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: ${PORT:9090}
